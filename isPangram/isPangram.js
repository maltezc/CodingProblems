"use strict";
// TODO: can we do this with O(log n)?
/**
 * Accepts a string
 * Returns whether is pangram
 *
 * 'abcdefghijklmnopqrstuvwxyz' => true
 * 'abc'=> false
 *
 * initialize strAlphabet
 * arrAlpha = strAlpha.split()
 *
 * while arrAlpha.length !== 0
 *  str.includes(arrAlpha.pop())
 *  if str.includes(arrAlpha.at[-1])
 *    arrAlpha.pop()
 *  else return false;
 *
 *  return true
 *
 *
 */

/**
 *
 */